import { useEffect, useState } from "react";

export default function quiz({ gameRoomId }) {
  const [que, setQue] = useState("");
  const [ans, setAns] = useState("");
  const [options, setOptions] = useState([]);

  useEffect(() => {}, []);
  return <div></div>;
}
